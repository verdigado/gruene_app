import 'package:flutter/widgets.dart';

class FlyerEdit extends StatelessWidget {
  const FlyerEdit({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
